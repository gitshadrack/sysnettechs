"use client";
import { useEffect,useState } from "react";
export function CookieConsent(){const [show,setShow]=useState(false);useEffect(()=>setShow(!localStorage.getItem("cookie-consent")),[]);if(!show)return null;const save=(v:string)=>{localStorage.setItem("cookie-consent",v);setShow(false)};return <div className="fixed inset-x-4 bottom-5 z-[60] mx-auto max-w-3xl rounded-2xl border border-slate-200 bg-white p-5 shadow-2xl dark:border-slate-700 dark:bg-slate-900"><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><p className="flex-1 text-sm">We use essential cookies and optional analytics to improve your experience. You control your choice.</p><div className="flex gap-2"><button onClick={()=>save("essential")} className="rounded-full border px-4 py-2 text-sm font-bold">Essential only</button><button onClick={()=>save("all")} className="rounded-full bg-brand-teal px-4 py-2 text-sm font-bold text-white">Accept all</button></div></div></div>}
